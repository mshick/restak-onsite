# --- Supabase ---
# These are the values printed by `pnpm db:start`.
NEXT_PUBLIC_SUPABASE_URL="http://127.0.0.1:54321"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# --- LLM ---
# Used by src/lib/reconcile/llm.ts (gpt-5-mini).
# Leave blank to run with a stub that flags every difference as "ambiguous".
OPENAI_API_KEY=""
